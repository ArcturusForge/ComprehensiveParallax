namespace Arcturus.Parallax
{
    /// <summary>
    /// If you havee created your own preset then add 
    /// </summary>
    public enum ParallaxPreset { InfiniteLoop, SlidingPane, DepthSlide }

    public enum ParallaxDirection { Vertical, Horizontal, Both }
}
